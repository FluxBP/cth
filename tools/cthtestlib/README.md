This is the Javascript test library.

To use it in a JS test `run` script:

```
require('CthTestLib');
```

This will simply populate the global scope of the test script with all the test library functions.
